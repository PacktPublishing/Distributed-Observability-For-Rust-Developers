# Products Service Configuration for Docker

[server]
# Server port (must match the port in otelmart/config.toml)
port = 3001

[database]
# PostgreSQL connection URL (using postgres service name in docker-compose)
url = "postgres://opentel_user:opentel_pass@postgres:5432/opentel_db"
# Maximum number of connections in the pool
max_connections = 5
